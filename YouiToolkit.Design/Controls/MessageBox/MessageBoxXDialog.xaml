<Window x:Class="YouiToolkit.Design.MessageBoxXDialog"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YouiToolkit.Design"
        mc:Ignorable="d"
        Title="MessageBox" Width="700" Height="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        x:Name="Msb">
    <Window.Resources>
        <ResourceDictionary Source="/YouiToolkit.Design;component/Resources/Converter.xaml"/>
    </Window.Resources>
    <Border Background="White"
            CornerRadius="3"
            Margin="5"
            MouseLeftButtonDown="BdrMain_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="LightGray"
                              BlurRadius="5"
                              ShadowDepth="0" />
        </Border.Effect>
        <Grid>
            <Grid x:Name="GrdStandard"
                  Visibility="Visible"
                  Margin="0,20,0,15">
                <Grid.Resources>
                    <Style TargetType="Button"
                           BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="local:ButtonHelper.CornerRadius"
                                Value="3" />
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid VerticalAlignment="Center"
                      Margin="0,30,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{Binding MessageBoxIcon,ElementName=Msb, Converter={StaticResource MsbCheckIconWidthConverter}}" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <local:MessageBoxXIcon MessageBoxIcon="{Binding MessageBoxIcon, ElementName=Msb}"
                                     RenderTransformOrigin="0.5,0.5"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Right"
                                     Thickness="3"
                                     Width="60"
                                     Height="60"
                                     Margin="0,0,20,0"/>
                    <Grid Grid.Column="1"
                          VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Title,ElementName=Msb}"
                                   FontSize="{Binding FontSize,ElementName=Msb,Converter={StaticResource Add10Converter}}"
                                   VerticalAlignment="Top"
                                   Margin="0,10,40,10" />
                        <ScrollViewer Grid.Row="1"
                                      local:ScrollViewerHelper.ScrollBarThickness="6"
                                      local:ScrollViewerHelper.ScrollBarCornerRadius="3"
                                      Margin="0,10,40,0"
                                      MaxHeight="{Binding MaxContentHeight,ElementName=Msb}"
                                      HorizontalAlignment="Left"
                                      VerticalScrollBarVisibility="Auto"
                                      Padding="0,0,10,0">
                            <TextBlock MaxWidth="{Binding MaxContentWidth,ElementName=Msb}"
                                       Text="{Binding Text,ElementName=Msb}"
                                       FontSize="{Binding FontSize,ElementName=Msb}"
                                       Foreground="Gray"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>

                    </Grid>
                </Grid>
                <Grid  Margin="0,0"
                       Grid.Row="1">
                    <StackPanel x:Name="StkButtonGroup1"
                                Orientation="Horizontal"
                                Height="35"
                                HorizontalAlignment="Right"
                                Margin="0,0,30,0">
                        <Button x:Name="buttonCancel" Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding CancelButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="Cancel"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                Margin="5,0"
                                Click="BtnCancel_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Cancel">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button x:Name="buttonNo" Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding NoButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="No"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                Margin="5,0"
                                Click="BtnNo_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="No">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button x:Name="buttonYes" Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding YesButton,ElementName=Msb}"
                                Tag="Yes"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                Margin="5,0"
                                Click="BtnYes_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Yes">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button x:Name="buttonOK" Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding OKButton,ElementName=Msb}"
                                Tag="OK"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                Margin="5,0"
                                Click="BtnOK_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="OK">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="StkButtonGroupReverse1"
                                Visibility="Collapsed"
                                Orientation="Horizontal"
                                Height="35"
                                HorizontalAlignment="Right"
                                Margin="0,0,30,0">
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding OKButton,ElementName=Msb}"
                                Tag="OK"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                Margin="5,0"
                                Click="BtnOK_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="OK">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding YesButton,ElementName=Msb}"
                                Tag="Yes"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                Margin="5,0"
                                Click="BtnYes_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Yes">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding NoButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="No"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                Margin="5,0"
                                Click="BtnNo_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="No">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding CancelButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="Cancel"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                Margin="5,0"
                                Click="BtnCancel_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Cancel">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                    </StackPanel>

                </Grid>
            </Grid>
            <Grid x:Name="GrdModern"
                  Visibility="Collapsed"
                  Margin="50,20,50,15">
                <Grid.Resources>
                    <Style TargetType="Button"
                           BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="local:ButtonHelper.CornerRadius"
                                Value="3" />
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid VerticalAlignment="Center"
                      Margin="0,30,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <local:MessageBoxXIcon MessageBoxIcon="{Binding MessageBoxIcon, ElementName=Msb}"
                                     Width="100"
                                     Height="100"
                                     Visibility="{Binding MessageBoxIcon,ElementName=Msb, Converter={StaticResource MsbCheckIconVisibilityConverter}}" />
                    <TextBlock Grid.Row="1"
                               Text="{Binding Title,ElementName=Msb}"
                               FontSize="{Binding FontSize,ElementName=Msb,Converter={StaticResource Add10Converter}}"
                               Margin="0,10"
                               HorizontalAlignment="Center" />
                    <ScrollViewer Grid.Row="2"
                                  local:ScrollViewerHelper.ScrollBarThickness="6"
                                  local:ScrollViewerHelper.ScrollBarCornerRadius="3"
                                  Margin="20,10"
                                  MaxHeight="{Binding MaxContentHeight,ElementName=Msb}"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="0,0,10,0">
                        <TextBlock MaxWidth="{Binding MaxContentWidth,ElementName=Msb}"
                                   Text="{Binding Text,ElementName=Msb}"
                                   HorizontalAlignment="Center"
                                   FontSize="{Binding FontSize,ElementName=Msb}"
                                   Foreground="Gray"
                                   TextWrapping="Wrap" />
                    </ScrollViewer>
                </Grid>
                <Grid Grid.Row="1">
                    <StackPanel x:Name="StkButtonGroup2"
                                Grid.Row="1"
                                Orientation="Horizontal"
                                Height="35"
                                HorizontalAlignment="Center">
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding CancelButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="Cancel"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                Margin="5,0"
                                Click="BtnCancel_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Cancel">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding NoButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="No"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                Margin="5,0"
                                Click="BtnNo_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="No">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding YesButton,ElementName=Msb}"
                                Tag="Yes"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                Margin="5,0"
                                Click="BtnYes_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Yes">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding OKButton,ElementName=Msb}"
                                Tag="OK"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                Margin="5,0"
                                Click="BtnOK_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="OK">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="StkButtonGroupReverse2"
                                Visibility="Collapsed"
                                Grid.Row="1"
                                Orientation="Horizontal"
                                Height="35"
                                HorizontalAlignment="Center">
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding OKButton,ElementName=Msb}"
                                Tag="OK"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                Margin="5,0"
                                Click="BtnOK_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="OK">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="{Binding ThemeBrush,ElementName=Msb}"
                                Content="{Binding YesButton,ElementName=Msb}"
                                Tag="Yes"
                                local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                Margin="5,0"
                                Click="BtnYes_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Yes">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding NoButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="No"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                Margin="5,0"
                                Click="BtnNo_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="No">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                        <Button Padding="40,0"
                                Background="#BBBBBB"
                                Content="{Binding CancelButton,ElementName=Msb}"
                                local:ButtonHelper.HoverBrush="#AAAAAA"
                                Tag="Cancel"
                                Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                Margin="5,0"
                                Click="BtnCancel_Click">
                            <Button.IsDefault>
                                <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                              ConverterParameter="Cancel">
                                    <Binding Path="DefaultButton"
                                             ElementName="Msb" />
                                    <Binding Path="MessageBoxButton"
                                             ElementName="Msb" />
                                </MultiBinding>
                            </Button.IsDefault>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
            <Grid x:Name="GrdClassic"
                  Visibility="Collapsed">
                <TextBlock Text="{Binding Title,ElementName=Msb}"
                           FontSize="{Binding FontSize,ElementName=Msb}"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           Margin="15,10,0,0" />
                <Grid 
                      Margin="0,20,0,15">
                    <Grid.Resources>
                        <Style TargetType="Button"
                               BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="local:ButtonHelper.CornerRadius"
                                    Value="3" />
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>
                    <Grid VerticalAlignment="Center"
                          Margin="0,30,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding MessageBoxIcon,ElementName=Msb, Converter={StaticResource MsbCheckIconWidthConverter}}" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <local:MessageBoxXIcon MessageBoxIcon="{Binding MessageBoxIcon, ElementName=Msb}"
                                         RenderTransformOrigin="0.5,0.5"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Right"
                                         Thickness="3"
                                         Width="60"
                                         Height="60"
                                         Margin="0,0,20,0">
                        </local:MessageBoxXIcon>
                        <ScrollViewer Grid.Column="1"
                                      VerticalAlignment="Center"
                                      local:ScrollViewerHelper.ScrollBarThickness="6"
                                      local:ScrollViewerHelper.ScrollBarCornerRadius="3"
                                      Margin="10,0,40,0"
                                      MaxHeight="{Binding MaxContentHeight,ElementName=Msb}"
                                      HorizontalAlignment="Left"
                                      VerticalScrollBarVisibility="Auto"
                                      Padding="0,0,10,0">
                            <TextBlock MaxWidth="{Binding MaxContentWidth,ElementName=Msb}"
                                       Text="{Binding Text,ElementName=Msb}"
                                       FontSize="{Binding FontSize,ElementName=Msb}"
                                       Foreground="Gray"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>

                    </Grid>
                    <Grid  Margin="0,0"
                           Grid.Row="1">
                        <StackPanel x:Name="StkButtonGroup3"
                                    Orientation="Horizontal"
                                    Height="35"
                                    HorizontalAlignment="Right"
                                    Margin="0,0,30,0">
                            <Button Padding="40,0"
                                    Background="#BBBBBB"
                                    Content="{Binding CancelButton,ElementName=Msb}"
                                    local:ButtonHelper.HoverBrush="#AAAAAA"
                                    Tag="Cancel"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                    Margin="5,0"
                                    Click="BtnCancel_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="Cancel">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="#BBBBBB"
                                    Content="{Binding NoButton,ElementName=Msb}"
                                    local:ButtonHelper.HoverBrush="#AAAAAA"
                                    Tag="No"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                    Margin="5,0"
                                    Click="BtnNo_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="No">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="{Binding ThemeBrush,ElementName=Msb}"
                                    Content="{Binding YesButton,ElementName=Msb}"
                                    Tag="Yes"
                                    local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                    Margin="5,0"
                                    Click="BtnYes_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="Yes">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="{Binding ThemeBrush,ElementName=Msb}"
                                    Content="{Binding OKButton,ElementName=Msb}"
                                    Tag="OK"
                                    local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                    Margin="5,0"
                                    Click="BtnOK_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="OK">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                        </StackPanel>
                        <StackPanel x:Name="StkButtonGroupReverse3"
                                    Visibility="Collapsed"
                                    Orientation="Horizontal"
                                    Height="35"
                                    HorizontalAlignment="Right"
                                    Margin="0,0,30,0">
                            <Button Padding="40,0"
                                    Background="{Binding ThemeBrush,ElementName=Msb}"
                                    Content="{Binding OKButton,ElementName=Msb}"
                                    Tag="OK"
                                    local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=OK}"
                                    Margin="5,0"
                                    Click="BtnOK_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="OK">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="{Binding ThemeBrush,ElementName=Msb}"
                                    Content="{Binding YesButton,ElementName=Msb}"
                                    Tag="Yes"
                                    local:ButtonHelper.HoverBrush="{Binding ThemeBrush,ElementName=Msb}"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Yes}"
                                    Margin="5,0"
                                    Click="BtnYes_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="Yes">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="#BBBBBB"
                                    Content="{Binding NoButton,ElementName=Msb}"
                                    local:ButtonHelper.HoverBrush="#AAAAAA"
                                    Tag="No"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=No}"
                                    Margin="5,0"
                                    Click="BtnNo_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="No">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                            <Button Padding="40,0"
                                    Background="#BBBBBB"
                                    Content="{Binding CancelButton,ElementName=Msb}"
                                    local:ButtonHelper.HoverBrush="#AAAAAA"
                                    Tag="Cancel"
                                    Visibility="{Binding MessageBoxButton,ElementName=Msb,Converter={StaticResource MsbButtonVisibilityConverter},ConverterParameter=Cancel}"
                                    Margin="5,0"
                                    Click="BtnCancel_Click">
                                <Button.IsDefault>
                                    <MultiBinding Converter="{StaticResource MsbButtonIsDefaultConverter}"
                                                  ConverterParameter="Cancel">
                                        <Binding Path="DefaultButton"
                                                 ElementName="Msb" />
                                        <Binding Path="MessageBoxButton"
                                                 ElementName="Msb" />
                                    </MultiBinding>
                                </Button.IsDefault>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
